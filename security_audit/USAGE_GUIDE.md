# Руководство по использованию Security Audit Framework

## Быстрый старт

### 1. Установка зависимостей

```bash
pip install pyyaml
```

### 2. Настройка конфигурации

Отредактируйте `config.yaml` для вашего целевого приложения:

```yaml
target_application:
  name: "Ваше приложение"
  version: "X.X.X"
  base_path: "/путь/к/приложению"
```

### 3. Запуск полного аудита

```bash
cd security_audit
python3 audit_main.py
```

Результаты будут сохранены в директории `results/`.

## Пошаговое выполнение

Вы можете запускать отдельные фазы аудита:

### Фаза 1: Картирование точек входа

```bash
python3 phase1_entrypoint_mapper.py --config config.yaml --output results/entrypoints.json
```

**Результат:** Полное картирование всех внешне достижимых точек входа.

### Фаза 2: Трассировка потока данных

```bash
python3 phase2_dataflow_tracer.py --config config.yaml --entrypoints results/entrypoints.json --output results/dataflows.json
```

**Результат:** Трассировка каждого параметра от источника до стока.

### Фаза 3: Фильтр устранения контроля

```bash
python3 phase3_control_filter.py --config config.yaml --dataflows results/dataflows.json --output-filtered results/filtered_flows.json --output-discarded results/discarded_flows.json
```

**Результат:** Отбрасывание потоков с устранённым контролем пользователя.

### Фаза 4: Анализ эксплуатируемости

```bash
python3 phase4_exploitability.py --config config.yaml --filtered results/filtered_flows.json --output results/vulnerabilities.json
```

**Результат:** Идентификация эксплуатируемых уязвимостей.

### Фаза 5: Анализ цепочек

```bash
python3 phase5_chain_analyzer.py --config config.yaml --vulns results/vulnerabilities.json --output results/exploit_chains.json
```

**Результат:** Построение цепочек эксплуатации.

### Генерация отчёта

```bash
python3 report_generator.py --config config.yaml --results-dir results --output results/SECURITY_AUDIT_REPORT.md
```

**Результат:** Полный отчёт в формате Markdown на русском языке.

## Структура результатов

После выполнения аудита в директории `results/` будут созданы следующие файлы:

```
results/
├── entrypoints.json             # Фаза 1: Точки входа
├── dataflows.json               # Фаза 2: Потоки данных
├── filtered_flows.json          # Фаза 3: Отфильтрованные потоки
├── discarded_flows.json         # Фаза 3: Отброшенные потоки
├── vulnerabilities.json         # Фаза 4: Уязвимости
├── exploit_chains.json          # Фаза 5: Цепочки
└── SECURITY_AUDIT_REPORT.md     # Финальный отчёт
```

## Интерпретация результатов

### Уровни серьёзности

- **CRITICAL** 🔴 - Требует немедленного устранения
- **HIGH** 🟠 - Высокий приоритет
- **MEDIUM** 🟡 - Средний приоритет
- **LOW** 🟢 - Низкий приоритет

### Уверенность в результатах

- **HIGH** - Уязвимость доказана с высокой уверенностью
- **MEDIUM** - Уязвимость вероятна, требует дополнительной проверки
- **LOW** - Потенциальная уязвимость, требует ручной верификации

### Контроль пользователя

- **YES** - Пользователь сохраняет полный контроль над данными
- **PARTIAL** - Контроль частично ограничен (например, HTML-кодирование)
- **NO** - Контроль полностью устранён (например, приведение к int)

## Настройка конфигурации

### Расширение списка опасных стоков

В `config.yaml` добавьте новые функции в секцию `dangerous_sinks`:

```yaml
dangerous_sinks:
  sql:
    - your_custom_sql_function
  command:
    - your_custom_exec_function
```

### Расширение списка источников ввода

```yaml
user_input_sources:
  - $_GET
  - $_POST
  - $your_custom_input
```

### Настройка паттернов точек входа

```yaml
entrypoint_patterns:
  http_controllers:
    - "custom/*.php"
    - "api/v*/endpoint.php"
```

## Расширенные сценарии

### Аудит только конкретной директории

Измените `base_path` в `config.yaml`:

```yaml
target_application:
  base_path: "/path/to/app/admin"
```

### Фокусировка на конкретных классах уязвимостей

Закомментируйте ненужные категории стоков в `config.yaml`:

```yaml
dangerous_sinks:
  sql:
    - mysql_query
  # command:  # Игнорировать command injection
  #   - exec
```

### Изменение глубины трассировки

```yaml
scan_settings:
  max_trace_depth: 20  # Увеличить для более глубокого анализа
```

## Производительность

### Оптимизация для больших кодовых баз

1. Ограничьте сканируемые директории:

```yaml
scan_settings:
  exclude_directories:
    - vendor
    - node_modules
    - tests
    - cache
```

2. Установите таймаут для анализа файлов:

```yaml
scan_settings:
  file_analysis_timeout: 10  # секунды
```

### Параллельная обработка

Для больших проектов можно разделить анализ по директориям и запустить параллельно несколько экземпляров с разными конфигурациями.

## Интеграция в CI/CD

### GitHub Actions

```yaml
name: Security Audit
on: [push, pull_request]

jobs:
  security-audit:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Setup Python
        uses: actions/setup-python@v2
        with:
          python-version: '3.9'
      - name: Install dependencies
        run: pip install pyyaml
      - name: Run security audit
        run: |
          cd security_audit
          python3 audit_main.py
      - name: Upload results
        uses: actions/upload-artifact@v2
        with:
          name: security-audit-results
          path: security_audit/results/
```

### GitLab CI

```yaml
security-audit:
  stage: test
  script:
    - pip install pyyaml
    - cd security_audit
    - python3 audit_main.py
  artifacts:
    paths:
      - security_audit/results/
    expire_in: 1 week
```

## Устранение неполадок

### Проблема: "File not found" ошибки

**Решение:** Проверьте, что `base_path` в `config.yaml` указывает на корректную директорию.

### Проблема: Слишком много false positives

**Решение:** 
1. Расширьте список санитизирующих функций в `config.yaml`
2. Увеличьте порог уверенности в `report_settings`

### Проблема: Пропущенные уязвимости

**Решение:**
1. Добавьте больше паттернов стоков в `dangerous_sinks`
2. Увеличьте `max_trace_depth`
3. Проверьте `exclude_directories` - возможно, исключены важные файлы

### Проблема: Медленная работа

**Решение:**
1. Добавьте больше директорий в `exclude_directories`
2. Уменьшите `file_analysis_timeout`
3. Сфокусируйтесь на конкретных директориях

## Лучшие практики

1. **Регулярные аудиты:** Запускайте аудит при каждом значительном изменении кода
2. **Версионирование результатов:** Сохраняйте результаты каждого аудита для сравнения
3. **Ручная верификация:** Всегда проверяйте критичные уязвимости вручную
4. **Приоритизация:** Начинайте с устранения CRITICAL и HIGH уязвимостей
5. **Документирование:** Ведите журнал устранённых уязвимостей

## Ограничения

Данный инструмент:

- ✅ Анализирует статический код
- ✅ Находит прямые потоки данных
- ✅ Идентифицирует очевидные уязвимости
- ❌ Не выполняет динамический анализ
- ❌ Не покрывает все возможные векторы атак
- ❌ Может пропустить сложные цепочки вызовов
- ❌ Не заменяет ручное тестирование на проникновение

## Контакты и поддержка

Для вопросов и предложений по улучшению инструмента:

- Создайте issue в репозитории
- Отправьте pull request с улучшениями

## Лицензия

Инструмент предназначен исключительно для авторизованного тестирования безопасности.

**ВАЖНО:** Использование данного инструмента для несанкционированного тестирования безопасности является незаконным.
